{
  "name": "figma-plugin",
  "private": true,
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=dist/code.js --target=es2017",
    "watch": "esbuild code.ts --bundle --outfile=dist/code.js --watch --target=es2017"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.130.0",
    "esbuild": "^0.24.0"
  }
}
