# URL of the WebSocket proxy service
# Change this if the proxy runs on a different host or port
PROXY_URL=http://localhost:3000

# Port for the MCP server
MCP_PORT=3001

# Optional: pre-shared API key for multiplexing (auto-generated if not set)
# API_KEY=
